#include "/Engine/Public/Platform.ush"


int SizeX;
int SizeY;
RWStructuredBuffer<float> Height;

int GetIndex(int X, int Y)
{
	return X * SizeX + Y;
};

[numthreads(32, 32, 1)]
void MainComputeShader(uint3 DTid : SV_DispatchThreadID)
{   
	Height[GetIndex(DTid.x, DTid.y)] = 1000.0f;
}